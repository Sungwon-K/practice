svg(scalar vector graphic)
=>웹에서 vector graphic을 구현하는데 사용
=>vector graphic은 flash, illustrator 등에서 사용하는 포맷
=>이미지의 확대,축소시  이미지품질이 떨어지지 않음.
    반대로 gif,jpg,png 등은 확대, 축소시에 이미지품질이 떨어짐

=>XML문법을 작성
*직접코딩하기 보다는 illustrator 등으로 그려서 svg로 변환함

Event Bubbling
=> 자식 엘리먼트에서 이벤트가 발생하면 부모 엘리먼트로 이벤트가 확장(bubbling)됨
=> e.stopPropagation()으로 이벤트 버블링을 막을 수 있음.